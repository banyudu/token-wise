# Context

Project service IDs and environments. IDs/URLs only — never secrets.

```yaml
# Issues and PRs are tracked on GitHub; this repo has no Linear project.
github:
  repo: banyudu/token-wise
```
