<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Developer ID / direct-distribution build: NOT sandboxed, so the app has
         normal user-file access (reads ~/.claude and ~/.codex directly).
         Hardened runtime is enabled automatically for notarization. -->
    <key>com.apple.security.network.client</key>
    <true/>
</dict>
</plist>
