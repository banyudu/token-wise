<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.app-sandbox</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.files.user-selected.read-only</key>
    <true/>
    <!-- `~/.claude` and `~/.codex` are read via a user-granted folder picker and a
         persisted security-scoped bookmark (see src/grants.*), NOT via a temporary
         entitlement. Apple rejects the temporary-exception entitlement (guideline
         2.4.5(i)), so it is intentionally omitted. -->
    <key>com.apple.application-identifier</key>
    <string>RYLS8UDY5D.com.banyudu.token-wise</string>
    <key>com.apple.developer.team-identifier</key>
    <string>RYLS8UDY5D</string>
</dict>
</plist>
